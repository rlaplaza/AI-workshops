ROBERT v 2.1.0 2026/02/25 19:06:58 
How to cite: Dalmau, D.; Alegre Requena, J. V. WIREs Comput Mol Sci. 2024, 14, e1733.

Command line used in ROBERT: python -m robert --csv_name "Classification.csv" --y "Outcome" --names "Name" --type "clas" --repeat_kfolds "1"


o  Starting generation of ML models with the GENERATE module

o  Database Classification_CURATE.csv loaded successfully, including:
   - 20 datapoints
   - 24 accepted descriptors
   - 1 ignored descriptors
   - 0 discarded descriptors

o  Starting heatmap scan with 4 ML models (['RF', 'GB', 'NN', 'ADAB']).
   Heatmap generation:
   o Starting BO-based hyperoptimization using the combined target:
                    
     1. 50% = MCC from a 1x repeated 5-fold CV (interpoplation)
                    
     2. 50% = MCC from the bottom or top (worst performing) fold in a sorted 5-fold CV (extrapolation)
                    

   - 1/4 - ML model: RF 
      o Using model-specific curated database: Classification_CURATE_RF.csv

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Best combined MCC (target) found in BO for RF (no PFI filter): 0.4

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Combined MCC for RF (with PFI filter): 0.44
   - 2/4 - ML model: GB 
      o Using model-specific curated database: Classification_CURATE_GB.csv

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Best combined MCC (target) found in BO for GB (no PFI filter): 0.49

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Combined MCC for GB (with PFI filter): 0.38
   - 3/4 - ML model: NN 
      o Using model-specific curated database: Classification_CURATE_NN.csv

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Best combined MCC (target) found in BO for NN (no PFI filter): 0.49

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Combined MCC for NN (with PFI filter): 0.51
   - 4/4 - ML model: ADAB 
      o Using model-specific curated database: Classification_CURATE_ADAB.csv

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Best combined MCC (target) found in BO for ADAB (no PFI filter): 0.66

o  Before hyperoptimization, 20% of the data (or 4 points at least) was separated as test set, using an even distribution of data points across the range of y values.
   o Combined MCC for ADAB (with PFI filter): 0.66

o  Heatmap_ML_models_No_PFI succesfully created in GENERATE/Raw_data

o  Heatmap_ML_models_PFI succesfully created in GENERATE/Raw_data

Time GENERATE: 47.62 seconds

